ip address show
ip a
ssh sameer@192.168.100.84
whoami
id
pwd
hostname
sudo su -
echo "Hello Linux"
echo $SHELL
sudo reboot
sudo shutdown now

############ day 2 ##################################################

ip link
ip route
ip route | grep default
cat /etc/resolv.conf
resolvectl status

touch sameer.txt
ls
   -a all
   -h human friendly
   -l long listing
   nano sameer.txt
   ctrl + o # to save
   ctrl +x # to save and exit
   52  ls -l 
   53  ls -lh
   54  ls -lh /etc
   55  ls -la
   56  file sameer.txt # show the file type
   57  file .noufal 
   58  touch linux #  create empty file
   59  file linux
   60  echo "Linux is a awsome operating system" # show the output in the screen
   61  echo "Linux is a awsome operating system" > linux # redirect the output to linux file
   62  cat linux # display file content in the screen
   63  file linux 
   64  mkdir friends # create directory
   65  ls # list
   66  ls -l # long listing
   67  cd friends/ # change directory
   68  pwd
   69  mkdir abc 
   70  ls
  
   
   80  cd .. # cd to parent directory
   81  cd # cd to home directory
   82  pwd
   83  cd - # cd to previous directory
   84  cd /etc/ssh/
   85  cd
   86  pwd
   87  cd -
   88  pwd
   89  cd
   90  cd -
   91  cd ..
   92  cd ~
   93  history



####################### day3 ####################################
  111  lsblk
  112  df
  113  df -h
  114  free -h
  115  clear
  116  lsblk
  117  df -h
  118  free -h
  119  cat /etc/os-release 
  120  ls
  121  touch abc.txt
  122  ls
  123  ls -l
  124  touch sameer.txt 
  125  ls -l
  126  touch --help
  127  clear
  128  sudo cat /etc/ssh/sshd_config
  129  sudo head /etc/ssh/sshd_config
  130  sudo head -n 5 /etc/ssh/sshd_config
  131  sudo head -n 20 /etc/ssh/sshd_config
  132  clear
  133  sudo du /
  134  sudo du -h / | sort -rh
  135  sudo du -h / | sort -rh | head 
  136  clear
  137  du -h /
  138  du -h / | sort -rh
  139  du -h / | sort -rh | head -20 
  140  sudo du -h / | sort -rh | head -20 
  141  clear
  142  sudo tail /etc/ssh/sshd_config
  143  sudo tail -n 5 /etc/ssh/sshd_config
  144  sudo tail -5 /etc/ssh/sshd_config
  145  sudo head -5 /etc/ssh/sshd_config
  146  clear
  147  mkdir techminds
  148  ls
  149  rmdir techminds/
  150  clear
  151  mkdir techminds
  152  rmdir techminds/
  153  mkdir techminds
  154  cd techminds/
  155  mkdir noufal
  156  cd ..
  157  rmdir techminds/
  158  cd techminds/
  159  ls
  160  rmdir noufal/
  161  cd ..
  162  mkdir techminds/
  163  rmdir techminds/
  164  ls
  165  clear
  166  mkdir -p techminds/noufal/linuxdata
  167  ls -l
  168  cd techminds/
  169  ls -l
  170  cd noufal/
  171  ls
  172  cd linuxdata/
  173  ls
  174  cd ..
  175  cd /
  176  mkdir /home/sameer/ksa
  177  pwd
  178  ls
  179  ls /home/sameer/
  180  cd
  181  clear
  182  ls
  183  cp sameer.txt ksa/
  184  ls
  185  ls ksa/
  186  cp sameer.txt sameer.txt.backup
  187  ls
  188  cp sameer.txt /tmp/
  189  rm sameer.txt.backup 
  190  ls
  191  mv sameer.txt abc.txt
  192  ls
  193  abc.txt linux 
  194  mv abc.txt linux 
  195  ls
  196  cat linux 
  197  clear
  198  ls
  199  mv linux techminds/
  200  ls
  201  touch abc.txt
  202  rm abc.txt 
  203  ls
  204  rmdir techminds/
  205  rm -rf techminds/
